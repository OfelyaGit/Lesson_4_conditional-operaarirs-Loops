// // //Find the sign of product of three numbers without multiplication operator. Display the specified sign.
// let arr = [-14, 5, 0];
// let isNegative = 0;
// for (let i = 0; i < arr.length; i++) {
//   if (arr[i] < 0) {
//     isNegative += 1;
//   } else if (arr[i] === 0) {
//     alert("unsigned");
//   }
// }
// if (isNegative > 0 && isNegative % 2 === 0) {
//   alert('the sign is "+"');
// } else if (isNegative > 0 && isNegative % 2 !== 0) {
//   alert('the sign is "-"');
// }
