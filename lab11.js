// // /*Insert a number. Calculate product and sum of the digits of the number. If product is divisible by the
// // sum, print the quotient, otherwise print the remainder.*/

// let number = +prompt("enter a number");
// sum_NumDigits = 0;
// prod_NumDigits = 1;
// for (let i = 1; i < String(number).length + 1; i++) {
//   sum_NumDigits = sum_NumDigits + i;
//   prod_NumDigits = prod_NumDigits * i;
// }
// console.log(sum_NumDigits);
// console.log(prod_NumDigits);

// if (prod_NumDigits % sum_NumDigits === 0) {
//   alert("'Quotient is'" + " " + (prod_NumDigits % sum_NumDigits));
// } else if (prod_NumDigits % sum_NumDigits !== 0) {
//   alert("'Remainder' is " + " " + (prod_NumDigits % sum_NumDigits));
// } else {
//   alert("'Can not calculate'");
// }

// //? 
